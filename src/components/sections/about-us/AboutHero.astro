---
// About Hero Section - Qui sommes-nous page

const keyDates = [
  { year: "2005", description: "Création de Smice" },
  { year: "2010", description: "Création de la 1ère plateforme et de la Smice-App" },
  { year: "2019", description: "Création de la V2 Smice Plus" },
  { year: "2025", description: "Les 20 ans de Smice et de la V3 de la plateforme" },
];
---

<section class="about-hero" id="about-hero">
  <div class="about-hero-container">
    <div class="about-hero-content">
      <h1 class="about-hero-title">
        20 ans d'expertise au service d'une seule ambition : rendre la qualité 
        <span class="highlight">simple</span>, <span class="highlight">fiable</span> et <span class="highlight-accessible">accessible<img src="/images/about/hero-section/subline_accesible.png" alt="" class="subline-accessible" /></span>.
      </h1>
      <p class="about-hero-description">
        Depuis 2005, Smice fait avancer l'expérience client avec une méthode qui marche, une communauté qui s'implique et une plateforme qui pilote la qualité en temps réel.
      </p>
      <a href="#mission" class="btn-secondary">En savoir plus</a>
    </div>
    
    <div class="about-hero-image">
      <div class="image-collage">
        <div class="badge-20">
          <img src="/images/about/hero-section/20_ans.png" alt="20 ans" class="badge-image" />
        </div>
        <div class="photo-wrapper">
          <img src="/images/about/hero-section/julien.png" alt="L'équipe Smice" class="main-image" />
        </div>
      </div>
    </div>
  </div>
  
  <!-- Key Dates Section -->
  <div class="key-dates-section">
    <h2 class="key-dates-title">
      Les <span class="underline">dates clés<img src="/images/about/hero-section/subline_date.png" alt="" class="subline-datecles" /></span> de Smice
    </h2>
    
    <div class="key-dates-grid">
      {keyDates.map((item) => (
        <div class="date-item">
          <span class="date-year">{item.year}</span>
          <p class="date-description">{item.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .about-hero {
    background-color: #F3EFF6;
    padding-top: 130px;
    padding-bottom: 80px;
    border-radius: 0 0 40px 40px;
  }

  .about-hero-container {
    max-width: 1224px;
    width: 100%;
    margin: 0 auto;
    padding: 0 24px;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 60px;
    align-items: center;
  }

  .about-hero-content {
    max-width: 520px;
  }

  .about-hero-title {
    font-size: 34px;
    font-weight: 800;
    color: #435888;
    line-height: 1.3;
    margin: 0 0 24px 0;
  }

  .about-hero-title .highlight {
    color: #9B8AAD;
  }

  .about-hero-title .highlight-accessible {
    color: #9B8AAD;
    position: relative;
    display: inline-block;
  }

  .subline-accessible {
    position: absolute;
    left: 0;
    bottom: -2px;
    width: 100%;
    height: auto;
    pointer-events: none;
  }

  .about-hero-description {
    font-size: 16px;
    color: var(--text-gray);
    line-height: 1.7;
    margin: 0 0 32px 0;
  }

  .btn-secondary {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 157px;
    height: 45px;
    padding: 8px 16px;
    background-color: #9B8AAD;
    color: white;
    font-size: 14px;
    font-weight: 600;
    border-radius: 6px;
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .btn-secondary:hover {
    background-color: #8a7a9c;
    transform: translateY(-2px);
  }

  /* Image Collage */
  .about-hero-image {
    display: flex;
    justify-content: flex-end;
  }

  .image-collage {
    position: relative;
    width: 100%;
    max-width: 550px;
    display: flex;
    justify-content: flex-end;
  }

  .photo-wrapper {
    border-radius: 16px;
    padding: 8px;
    background-color: #FFFFFE;
    display: inline-block;
    max-height: 500px;
  }

  .main-image {
    width: 100%;
    height: 100%;
    max-height: 434px;
    border-radius: 12px;
    object-fit: cover;
    display: block;
  }

  .badge-20 {
    position: absolute;
    bottom: -20px;
    left: 60px;
    width: 100px;
    height: 100px;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 10;
  }

  .badge-image {
    width: 100%;
    height: 100%;
    object-fit: contain;
  }

  /* Key Dates Section */
  .key-dates-section {
    max-width: 1224px;
    width: 100%;
    margin: 80px auto 0;
    text-align: center;
    border-radius: 0 0 40px 40px;
  }

  .key-dates-title {
    font-size: 20px;
    font-weight: 700;
    color: #504C54;
    margin: 0 0 50px 0;
  }

  .key-dates-title .underline {
    color: #6989D4;
    position: relative;
    display: inline-block;
    text-decoration: none;
  }

  .subline-datecles {
    position: absolute;
    left: 0;
    bottom: 0;
    width: 100%;
    height: auto;
    pointer-events: none;
  }

  .key-dates-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 30px;
  }

  .date-item {
    text-align: center;
  }

  .date-year {
    display: block;
    font-size: 32px;
    font-weight: 800;
    color: #6989D4;
    margin-bottom: 12px;
  }

  .date-description {
    font-size: 20px;
    font-weight: 400;
    color: #504C54;
    line-height: 1.5;
    margin: 0;
  }

  /* ===== Tablet Responsive ===== */
  @media (max-width: 1024px) {
    .about-hero-container {
      gap: 40px;
    }

    .about-hero-title {
      font-size: 30px;
    }

    .key-dates-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 40px 60px;
    }

    .date-year {
      font-size: 40px;
    }
  }

  /* ===== Mobile Responsive ===== */
  @media (max-width: 768px) {
    .about-hero {
      padding-top: 100px;
      padding-bottom: 60px;
    }

    .about-hero-container {
      grid-template-columns: 1fr;
      gap: 40px;
      text-align: center;
    }

    .about-hero-content {
      max-width: 100%;
    }

    .about-hero-title {
      font-size: 26px;
    }

    .about-hero-description {
      font-size: 15px;
    }

    .image-collage {
      max-width: 350px;
    }

    .badge-20 {
      width: 70px;
      height: 70px;
      bottom: -15px;
    }

    .badge-number {
      font-size: 28px;
    }

    .key-dates-section {
      margin-top: 60px;
    }

    .key-dates-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 30px;
    }

    .date-year {
      font-size: 36px;
    }

    .date-description {
      font-size: 13px;
    }
  }

  /* ===== Small Mobile ===== */
  @media (max-width: 480px) {
    .about-hero-title {
      font-size: 22px;
    }

    .key-dates-grid {
      grid-template-columns: 1fr 1fr;
      gap: 24px;
    }

    .date-year {
      font-size: 32px;
    }
  }
</style>
